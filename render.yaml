services:
  - type: web
    name: taxi-saas-backend
    env: node
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: cd apps/api && npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: GOOGLE_MAPS_API_KEY
        sync: false
